### lwIP NTP Example

Starts lwIP with DHCP and SNTP service flags, then waits for the lwIP SNTP clock to be set.

This uses the public release API and depends on the active network/DHCP setup providing an NTP server.
