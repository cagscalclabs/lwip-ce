### lwIP TCP Echo Example

Connects to tcpbin's static IPv4 address (`45.79.112.203:4242`), writes a short line, and checks that the same bytes are echoed back.

This requires the lwIP app to be installed on the calculator before running the example.
