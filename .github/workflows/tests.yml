name: Unit Tests

on:
  push:
    branches: ['tls']
    paths: ['src/tls/**', 'tests/**']
  workflow_dispatch:

env:
  AUTOTESTER_ROM: ${{github.workspace}}/CE.rom
  LWIP_TESTS: ${{github.workspace}}/tests
  CEMU_PATH: ${{github.workspace}}/CEmu
  CEMU_ROM_PATH: ${{secrets.CL_SECRETS_URL}}/CE.rom
  SECRETS_TOKEN: ${{secrets.CL_SECRETS_TOKEN}}
  CEDEV_BIN: ${{github.workspace}}/CEdev/bin

jobs:
  test_core:
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: Checkout TLS branch
        uses: actions/checkout@v4
        with:
          ref: 'tls'
          submodules: true

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y make tar wget qt5-qmake qtbase5-dev xvfb

      - name: Download and extract CEdev
        run: |
          curl -sL github.com/CE-Programming/toolchain/releases/download/v14.0/CEdev-Linux.tar.gz | tar xz

      - name: Add CEdev to PATH
        run: |
          echo "${{github.workspace}}/CEdev/bin" >> $GITHUB_PATH
          echo $GITHUB_PATH

      - name: Download CEmu Rom
        run: |
          HTTP_CODE=$(curl -A "github/workflows" \
            -H "Authorization: Bearer ${{ env.SECRETS_TOKEN }}" \
            -w "%{http_code}" \
            -o ${{ env.AUTOTESTER_ROM }} \
            -SL ${{env.CEMU_ROM_PATH}} \
            --retry 3 \
            --retry-delay 2)

          echo "HTTP response code: ${HTTP_CODE}"

          if [ "$HTTP_CODE" != "200" ]; then
            echo "ERROR: Download failed with HTTP code ${HTTP_CODE}"
            ls -lh ${{ env.AUTOTESTER_ROM }} || echo "No file created"
            exit 1
          fi

          if [ ! -f "${{ env.AUTOTESTER_ROM }}" ]; then
            echo "ERROR: ROM file not created"
            exit 1
          fi

          FILE_SIZE=$(stat -c%s "${{ env.AUTOTESTER_ROM }}" 2>/dev/null || stat -f%z "${{ env.AUTOTESTER_ROM }}")
          echo "Downloaded ROM size: ${FILE_SIZE} bytes"

      - name: Checkout CEmu
        uses: actions/checkout@v4
        with:
          repository: CE-Programming/CEmu
          ref: latest-stable
          path: ${{env.CEMU_PATH}}
          persist-credentials: false

      - name: Build CEmu
        run: make -j4 -C ${{env.CEMU_PATH}}/core

      - name: Build Autotester CLI
        run: make -j4 -C ${{env.CEMU_PATH}}/tests/autotester

      - name: Install Autotester CLI
        run: cmake -E copy ${{env.CEMU_PATH}}/tests/autotester/autotester ${{env.CEDEV_BIN}}/cemu-autotester

      - name: Verify Autotester Setup
        run: |
          echo "Testing autotester with xvfb..."
          echo "ROM path: ${{env.AUTOTESTER_ROM}}"
          ls -lh ${{env.AUTOTESTER_ROM}}
          xvfb-run -a ${{env.CEDEV_BIN}}/cemu-autotester --help || echo "Autotester help failed (may be normal)"

      - name: Run Core Unit Tests
        run: |
          total_tests=0
          total_passed=0
          total_failed=0
          start_time=$(($(date +%s%N)/1000000))

          while IFS= read -r -d '' test; do
            status=0
            test_dir=$(dirname "$test")
            if [ "$(basename "$test_dir")" = "tls_timing" ]; then
              continue
            fi
            test_start=$(($(date +%s%N)/1000000))
            make -C "$test_dir" clean
            make -C "$test_dir" all test || ((status=$?))
            test_done=$(($(date +%s%N)/1000000))
            test_duration=$((test_done - test_start))
            (( total_tests += 1 ))
            if [ $status -eq 0 ]; then
              (( total_passed += 1 ))
              test_status="passed"
            else
              (( total_failed += 1 ))
              test_status="failed"
            fi
            echo -e "{\"name\":\"$(basename "$test_dir")\",\"status\":\"$test_status\",\"duration\":\"$test_duration\"}" > "$test_dir/result.json"
          done < <(find ${{env.LWIP_TESTS}} -name autotest.json -print0)

          end_time=$(($(date +%s%N)/1000000))
          echo -e "\"summary\":{\"tests\":\"$total_tests\",\"passed\":\"$total_passed\",\"failed\":\"$total_failed\",\"pending\":\"0\",\"skipped\":\"0\",\"other\":\"0\",\"start\":\"$start_time\",\"stop\":\"$end_time\"}" > ${{github.workspace}}/summary.json
          cmake -E echo "$total_tests core modules tested, $total_passed passed, $total_failed failed!"
          exit $total_failed

      - name: Generate Core JSON Results
        if: always()
        run: |
          echo -e "{\"results\":{" > ${{github.workspace}}/results-core.json
          cat ${{github.workspace}}/summary.json >> ${{github.workspace}}/results-core.json
          echo -e ",\"tests\":[" >> ${{github.workspace}}/results-core.json
          find ${{env.LWIP_TESTS}} -name result.json -print0 | {
            tmp_json=""
            while read -rd '' test; do
              test_dir=$(dirname "$test")
              if [ "$(basename "$test_dir")" = "tls_timing" ]; then
                continue
              fi
              tmp_json="$tmp_json,$(cat $test_dir/result.json)"
              rm $test_dir/result.json
            done
            tmp_json="${tmp_json:1}"
            echo $tmp_json >> ${{github.workspace}}/results-core.json
          }
          echo -e "]" >> ${{github.workspace}}/results-core.json
          echo -e "}}" >> ${{github.workspace}}/results-core.json
          rm ${{github.workspace}}/summary.json

      - name: Publish Core Test Summary
        if: always()
        run: |
          echo "## Core Test Results Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          total=$(jq -r '.results.summary.tests' ${{github.workspace}}/results-core.json)
          passed=$(jq -r '.results.summary.passed' ${{github.workspace}}/results-core.json)
          failed=$(jq -r '.results.summary.failed' ${{github.workspace}}/results-core.json)

          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Total Tests | $total |" >> $GITHUB_STEP_SUMMARY
          echo "| ✅ Passed | $passed |" >> $GITHUB_STEP_SUMMARY
          echo "| ❌ Failed | $failed |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### Individual Module Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          jq -r '.results.tests[] | .name' ${{github.workspace}}/results-core.json | while read test_name; do
            test_dir="$LWIP_TESTS/$test_name"
            test_count=$(jq '[.sequence[] | select(startswith("hashWait"))] | length' "$test_dir/autotest.json" 2>/dev/null || echo "1")

            status=$(jq -r ".results.tests[] | select(.name == \"$test_name\") | .status" ${{github.workspace}}/results-core.json)
            module_desc=$(jq -r '.description // empty' "$test_dir/autotest.json" 2>/dev/null)
            module_desc=${module_desc:-"(no description)"}
            module_desc=${module_desc//$'\n'/ }
            module_desc=${module_desc//|/\\|}

            if [ "$status" = "passed" ]; then
              status_icon="✅ Passed"
            else
              status_icon="❌ Failed"
            fi

            echo "" >> $GITHUB_STEP_SUMMARY
            echo "#### $test_name" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Module | Tests | Status | Description |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|--------|-------------|" >> $GITHUB_STEP_SUMMARY
            echo "| $test_name |  | $status_icon | $module_desc |" >> $GITHUB_STEP_SUMMARY

            jq -r '.sequence // [] | map(select(startswith("hashWait")))[]' "$test_dir/autotest.json" 2>/dev/null | while read -r seq_entry; do
              hash_id="${seq_entry#hashWait|}"
              hash_desc=$(jq -r ".hashes[\"$hash_id\"].description // \"(no description)\"" "$test_dir/autotest.json" 2>/dev/null)
              hash_desc=${hash_desc//$'\n'/ }
              hash_desc=${hash_desc//|/\\|}
              if [ "$status" = "passed" ]; then
                hash_status="✅ Passed"
              else
                hash_status="❌ Failed"
              fi
              echo "|  | $hash_id | $hash_status | $hash_desc |" >> $GITHUB_STEP_SUMMARY
            done
            if [ "$test_count" -eq 0 ]; then
              echo "|  | (none) | $status_icon | (no hash tests) |" >> $GITHUB_STEP_SUMMARY
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
          done

      - name: Upload Core Results Logfile
        uses: actions/upload-artifact@v4
        with:
          name: results-core.json
          path: ${{github.workspace}}/results-core.json

      - name: Remove CEmu Rom
        if: always()
        run: rm -f ${{env.AUTOTESTER_ROM}}

  test_timing:
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: Checkout TLS branch
        uses: actions/checkout@v4
        with:
          ref: 'tls'
          submodules: true

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y make tar wget qt5-qmake qtbase5-dev xvfb

      - name: Download and extract CEdev
        run: |
          curl -sL github.com/CE-Programming/toolchain/releases/download/v14.0/CEdev-Linux.tar.gz | tar xz

      - name: Add CEdev to PATH
        run: |
          echo "${{github.workspace}}/CEdev/bin" >> $GITHUB_PATH
          echo $GITHUB_PATH

      - name: Download CEmu Rom
        run: |
          HTTP_CODE=$(curl -A "github/workflows" \
            -H "Authorization: Bearer ${{ env.SECRETS_TOKEN }}" \
            -w "%{http_code}" \
            -o ${{ env.AUTOTESTER_ROM }} \
            -SL ${{env.CEMU_ROM_PATH}} \
            --retry 3 \
            --retry-delay 2)

          echo "HTTP response code: ${HTTP_CODE}"

          if [ "$HTTP_CODE" != "200" ]; then
            echo "ERROR: Download failed with HTTP code ${HTTP_CODE}"
            ls -lh ${{ env.AUTOTESTER_ROM }} || echo "No file created"
            exit 1
          fi

          if [ ! -f "${{ env.AUTOTESTER_ROM }}" ]; then
            echo "ERROR: ROM file not created"
            exit 1
          fi

      - name: Checkout CEmu
        uses: actions/checkout@v4
        with:
          repository: CE-Programming/CEmu
          ref: latest-stable
          path: ${{env.CEMU_PATH}}
          persist-credentials: false

      - name: Build CEmu
        run: make -j4 -C ${{env.CEMU_PATH}}/core

      - name: Build Autotester CLI
        run: make -j4 -C ${{env.CEMU_PATH}}/tests/autotester

      - name: Install Autotester CLI
        run: cmake -E copy ${{env.CEMU_PATH}}/tests/autotester/autotester ${{env.CEDEV_BIN}}/cemu-autotester

      - name: Verify Autotester Setup
        run: |
          echo "Testing autotester with xvfb..."
          echo "ROM path: ${{env.AUTOTESTER_ROM}}"
          ls -lh ${{env.AUTOTESTER_ROM}}
          xvfb-run -a ${{env.CEDEV_BIN}}/cemu-autotester --help || echo "Autotester help failed (may be normal)"

      - name: Run Timing Unit Test
        run: |
          total_tests=0
          total_passed=0
          total_failed=0
          start_time=$(($(date +%s%N)/1000000))

          test_dir="${{env.LWIP_TESTS}}/tls_timing"
          status=0
          test_start=$(($(date +%s%N)/1000000))
          make -C "$test_dir" clean
          make -C "$test_dir" all test || ((status=$?))
          test_done=$(($(date +%s%N)/1000000))
          test_duration=$((test_done - test_start))

          total_tests=1
          if [ $status -eq 0 ]; then
            total_passed=1
            test_status="passed"
          else
            total_failed=1
            test_status="failed"
          fi
          echo -e "{\"name\":\"tls_timing\",\"status\":\"$test_status\",\"duration\":\"$test_duration\"}" > "$test_dir/result.json"

          end_time=$(($(date +%s%N)/1000000))
          echo -e "\"summary\":{\"tests\":\"$total_tests\",\"passed\":\"$total_passed\",\"failed\":\"$total_failed\",\"pending\":\"0\",\"skipped\":\"0\",\"other\":\"0\",\"start\":\"$start_time\",\"stop\":\"$end_time\"}" > ${{github.workspace}}/summary.json
          cmake -E echo "timing module tested, $total_passed passed, $total_failed failed!"
          exit $total_failed

      - name: Generate Timing JSON Results
        if: always()
        run: |
          echo -e "{\"results\":{" > ${{github.workspace}}/results-timing.json
          cat ${{github.workspace}}/summary.json >> ${{github.workspace}}/results-timing.json
          echo -e ",\"tests\":[" >> ${{github.workspace}}/results-timing.json
          echo "$(cat ${{env.LWIP_TESTS}}/tls_timing/result.json)" >> ${{github.workspace}}/results-timing.json
          rm ${{env.LWIP_TESTS}}/tls_timing/result.json
          echo -e "]" >> ${{github.workspace}}/results-timing.json
          echo -e "}}" >> ${{github.workspace}}/results-timing.json
          rm ${{github.workspace}}/summary.json

      - name: Publish Timing Test Summary
        if: always()
        run: |
          echo "## Timing Test Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          test_name="tls_timing"
          test_dir="$LWIP_TESTS/$test_name"
          status=$(jq -r '.results.tests[] | select(.name == "tls_timing") | .status' ${{github.workspace}}/results-timing.json)
          module_desc=$(jq -r '.description // empty' "$test_dir/autotest.json" 2>/dev/null)
          module_desc=${module_desc:-"(no description)"}
          module_desc=${module_desc//$'\n'/ }
          module_desc=${module_desc//|/\\|}

          if [ "$status" = "passed" ]; then
            status_icon="✅ Passed"
          else
            status_icon="❌ Failed"
          fi

          echo "### Timing Module Result" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Module | Test | Status | Description |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|------|--------|-------------|" >> $GITHUB_STEP_SUMMARY
          echo "| $test_name |  | $status_icon | $module_desc |" >> $GITHUB_STEP_SUMMARY

          jq -r '.sequence // [] | map(select(startswith("hashWait")))[]' "$test_dir/autotest.json" 2>/dev/null | while read -r seq_entry; do
            hash_id="${seq_entry#hashWait|}"
            hash_desc=$(jq -r ".hashes[\"$hash_id\"].description // \"(no description)\"" "$test_dir/autotest.json" 2>/dev/null)
            hash_desc=${hash_desc//$'\n'/ }
            hash_desc=${hash_desc//|/\\|}
            if [ "$status" = "passed" ]; then
              hash_status="✅ Passed"
            else
              hash_status="❌ Failed"
            fi
            echo "|  | $hash_id | $hash_status | $hash_desc |" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### Timing Methodology (tls_timing)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Stability check: run repeated timing on identical input; pass requires standard deviation < 1.00% of mean." >> $GITHUB_STEP_SUMMARY
          echo "- Differential check: compare best/worst/random/edge input classes; fail only when mean cycle delta exceeds threshold and direction is consistent in >=75% of samples." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Primitive | Differential Threshold (cycles) |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|----------------------------------:|" >> $GITHUB_STEP_SUMMARY
          echo "| RNG | 64 |" >> $GITHUB_STEP_SUMMARY
          echo "| BYTES | 8 |" >> $GITHUB_STEP_SUMMARY
          echo "| SHA256 | 32 |" >> $GITHUB_STEP_SUMMARY
          echo "| HMAC | 32 |" >> $GITHUB_STEP_SUMMARY
          echo "| HKDF | 48 |" >> $GITHUB_STEP_SUMMARY
          echo "| PBKDF2 | 192 |" >> $GITHUB_STEP_SUMMARY
          echo "| AESGCM-E | 48 |" >> $GITHUB_STEP_SUMMARY
          echo "| AESGCM-DV | 64 |" >> $GITHUB_STEP_SUMMARY
          echo "| AESCBC-E | 48 |" >> $GITHUB_STEP_SUMMARY
          echo "| AESCBC-D | 48 |" >> $GITHUB_STEP_SUMMARY
          echo "| X25519 | 128 |" >> $GITHUB_STEP_SUMMARY

      - name: Upload Timing Results Logfile
        uses: actions/upload-artifact@v4
        with:
          name: results-timing.json
          path: ${{github.workspace}}/results-timing.json

      - name: Remove CEmu Rom
        if: always()
        run: rm -f ${{env.AUTOTESTER_ROM}}
